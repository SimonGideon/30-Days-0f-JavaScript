const myTodos = [];
myTodos.push('Buy Bread', 'Record Videos for youtube', 'Go to gym')
myTodos.forEach(function (todo , i) {
    console.log(`Your task no. ${i+1 } is: ${todo}`);

})
let myTodo = [];
    list = ['Buy Bread', 'Record Videos for youtube', 'Go to gym'];
    for (let index = 0; index < list.length; index++) {
        const element = list[index];
        myTodo.push(element);
        
    }
    console.log(myTodo);

    