// function that finds a square of a number.
function square(num){
    let squared = num*num;
    console.log(squared);
}
square(50);

// finding the sum function.
function sum(num1, num2){
    const add = num1 + num2;
    console.log(add);
}
sum(75, 50);